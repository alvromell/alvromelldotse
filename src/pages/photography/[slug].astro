---
import { getCollection, type CollectionEntry } from 'astro:content';
import {Image} from 'astro:assets';
import Gallery from "../../layouts/Gallery.astro";

type Props = {
    album: CollectionEntry<"albums">
}

export const getStaticPaths = async () => {
    const albums = await getCollection("albums");
    
    const paths = albums.map(album => {
        
        return {
            params: {
                slug: album.slug
            },
            props: {
                album,
            }
        }
    })

    return paths;
}
const { album } = Astro.props;
---

<Gallery title={"Alvs bilder från " + album.data.title} transition:animate="fade">
    <a href="/photography" aria-label="Back to albums" class="flex gap-2 text-slate-400 mt-4 font-light">
        <span class="text-sm">
            &lArr;
        </span>
        <span class="link-underline">Back to albums</span>
    </a>
    <h1 class="text-2xl md:text-3xl lg:text-5xl font-bold mt-6">{album.data.title}</h1>
    <span class="text-slate-400 font-light md:mt-2">{album.data.date}</span>
    <h2 class="md:text-xl mt-4 font-light">{album.data.description}</h2>
    <main>
        {album.data.images.map(img => (<Image alt={img.alt} src={img.img} class="mt-4 md:mt-8 p-2 bg-slate-50"/>))}
    </main>
</Gallery>
